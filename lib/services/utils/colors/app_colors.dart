import 'package:flutter/material.dart';

class AppColors {
  static const backgroundWhite = Colors.white;
  static const blackColor = Colors.black;
  static lightBlueWithopacity(opacity)
  {
    return Colors.lightBlue.withOpacity(opacity);
  }
  static yellowWithopacity(opacity)
  {
    return Colors.lightBlue.withOpacity(opacity);
  }
}
